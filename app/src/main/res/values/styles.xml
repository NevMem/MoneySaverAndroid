<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:textColor">@color/default_white_color</item>
        <item name="colorAccent">@color/specialColor</item>
        <item name="colorPrimary">@color/profile_color</item>
        <item name="colorPrimaryDark">@color/profile_color</item>

        <item name="android:windowActivityTransitions">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
    </style>

    <style name="SpecialButtonStyle">
        <item name="android:textColor">#fff</item>
        <item name="android:background">@color/mainColor</item>
    </style>

    <style name="LogOutButton" parent = "Base.Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:textColor">@color/dangerColor</item>
    </style>

</resources>
